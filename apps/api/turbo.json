{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build:pkg"],
      "outputs": ["dist/**"]
    },
    "start:dev": {
      "persistent": true
    }
  }
}
